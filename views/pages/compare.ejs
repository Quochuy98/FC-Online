<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/badge.css">
    <link rel="stylesheet" href="/css/seasons.css">
    <link rel="stylesheet" href="/css/upgrade.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#667eea',
                        secondary: '#764ba2',
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-white min-h-screen">
    <%- include('../partials/header') %>
    
    <div class="container mx-auto px-4 py-8">
        <!-- Header -->
        <div class="mb-8 p-6 border-b border-gray-200">
            <div class="flex items-center justify-between mb-4">
                <div class="flex items-center gap-4">
                    <a href="/" class="text-primary hover:text-blue-700 transition-colors">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                        </svg>
                    </a>
                    <h1 class="text-4xl font-bold text-gray-800">
                        ⚖️ So sánh cầu thủ
                    </h1>
                </div>
                <button 
                    onclick="clearAndBack()"
                    class="px-4 py-2 bg-red-500 hover:bg-red-600 text-white rounded-lg transition-colors"
                >
                    Xóa & Quay lại
                </button>
            </div>
            <p class="text-gray-600">So sánh chỉ số và OVR của các cầu thủ với Level, Team Color và Cấp thẻ</p>
        </div>

        <!-- Loading State -->
        <div id="loading" class="hidden text-center py-16">
            <div class="inline-block w-12 h-12 border-4 border-gray-200 border-t-primary rounded-full animate-spin mb-4"></div>
            <p class="text-gray-600 text-lg">Đang tải dữ liệu...</p>
        </div>

        <!-- Error State -->
        <div id="error" class="hidden text-center py-16 text-red-500 bg-red-50 rounded-lg border border-red-200"></div>

        <!-- Comparison Content -->
        <div id="compareContent" class="hidden">
            <!-- Position Selector -->
            <div class="mb-6 p-4 bg-gray-50 rounded-lg border border-gray-300">
                <label for="positionSelector" class="block text-sm font-semibold text-gray-700 mb-2">
                    🎯 Chọn vị trí để so sánh:
                </label>
                <select 
                    id="positionSelector" 
                    onchange="onPositionChange()"
                    class="w-full px-4 py-2 text-base font-semibold text-gray-700 bg-white border border-gray-300 rounded-lg focus:outline-none focus:ring-1 focus:ring-gray-400 cursor-pointer"
                >
                    <!-- Options will be populated by JS -->
                </select>
            </div>

            <!-- Players Comparison Grid -->
            <div id="playersGrid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                <!-- Players will be populated here -->
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>

    <script src="/js/api.js"></script>
    <script src="/js/compare.js"></script>
</body>
</html>
