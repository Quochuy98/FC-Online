<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Badge CSS for season sprites -->
    <link rel="stylesheet" href="/css/badge.css">
    
    <!-- Custom Tailwind Config -->
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#667eea',
                        secondary: '#764ba2',
                    }
                }
            }
        }
    </script>
    
    <style>
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        .animate-spin {
            animation: spin 1s linear infinite;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-primary to-secondary min-h-screen">
    <%- include('../partials/header') %>
    
    <div class="container mx-auto px-4 py-6 max-w-7xl">
    <!-- Page Title -->
    <div class="bg-white rounded-2xl shadow-xl p-8 mb-6 text-center">
        <h2 class="text-4xl font-bold text-primary mb-3">⚽ Tìm kiếm theo câu lạc bộ</h2>
        <p class="text-gray-600 text-lg">Tìm tất cả cầu thủ từng chơi cho cùng một câu lạc bộ</p>
    </div>

    <!-- Search Box -->
    <div class="bg-white rounded-2xl shadow-xl p-8 mb-6">
        <div class="flex flex-col sm:flex-row gap-3">
            <input 
                type="text" 
                id="clubName" 
                placeholder="Nhập tên câu lạc bộ (ví dụ: Arsenal, Real Madrid)"
                class="flex-1 px-5 py-4 border-2 border-gray-200 rounded-xl text-base focus:outline-none focus:ring-4 focus:ring-primary/20 focus:border-primary transition-all"
            >
            <button 
                id="searchBtn" 
                class="px-8 py-4 bg-primary text-white rounded-xl font-semibold hover:bg-primary/90 hover:-translate-y-0.5 hover:shadow-lg transition-all"
            >
                🔍 Tìm kiếm
            </button>
        </div>
        <p class="text-sm text-gray-500 mt-3">
            💡 Tip: Nhập tên câu lạc bộ chính xác như trong database (ví dụ: "Arsenal", "FC Barcelona", "Real Madrid")
        </p>
    </div>

    <!-- Results Section -->
    <div class="bg-white rounded-2xl shadow-xl p-8">
        <!-- Results Header -->
        <div class="mb-6 pb-6 border-b-2 border-gray-100">
            <h2 class="text-3xl font-bold text-gray-800">
                Kết quả <span id="resultCount" class="text-primary text-2xl">(0)</span>
            </h2>
            <p id="clubTitle" class="text-gray-600 mt-2 hidden"></p>
        </div>

        <!-- Loading State -->
        <div id="loading" class="hidden text-center py-16">
            <div class="inline-block w-12 h-12 border-4 border-gray-200 border-t-primary rounded-full animate-spin mb-4"></div>
            <p class="text-gray-600 text-lg">Đang tìm kiếm...</p>
        </div>

        <!-- Error State -->
        <div id="error" class="hidden text-center py-16 text-red-500 bg-red-50 rounded-xl"></div>

        <!-- Empty State -->
        <div id="emptyState" class="text-center py-16 text-gray-500">
            <svg class="w-24 h-24 mx-auto mb-4 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
            </svg>
            <p class="text-xl font-semibold mb-2">Tìm kiếm cầu thủ theo câu lạc bộ</p>
            <p class="text-gray-400">Nhập tên câu lạc bộ để bắt đầu</p>
        </div>

        <!-- Results Grid -->
        <div id="playersGrid" class="hidden space-y-3">
            <!-- Players will be populated here -->
        </div>
    </div>
</div>

<%- include('../partials/footer') %>

<script src="/js/api.js"></script>
<script src="/js/club-search.js"></script>
</body>
</html>
